pip install fastapi uvicorn groq python-dotenv


These packages are for:

fastapi → backend framework

uvicorn → server

groq → Groq LLM API

python-dotenv → to load API key safely


https://console.groq.com/docs/libraries  read this for groq documentation


What are Embeddings? (Simple Explanation)

Think of embeddings as numbers that represent text in a way the computer can understand.

Every piece of text → converted into a vector of numbers

Similar text → similar vectors

Different text → very different vectors